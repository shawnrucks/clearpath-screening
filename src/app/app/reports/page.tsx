import Link from "next/link"; import {rows} from "@/lib/clearpath"; import {PageHead} from "@/components/Portal";
export default function Reports(){const d=rows("SELECT * FROM cp_reports ORDER BY id DESC");return <div className="page"><PageHead eyebrow="REPORTING" title="Reports" subtitle="Candidate screening reports and manually prepared operations briefs." actions={<Link href="/app/reports/operations" className="btn primary">+ Create Operations Report</Link>}/><div className="report-type-grid"><section className="card"><span>▣</span><h2>Candidate Screening Reports</h2><p>Review completed searches, results, discrepancies, notices, and report versions.</p><b>12 completed reports</b><button className="btn outline">Browse Reports →</button></section><section className="card"><span>▥</span><h2>Operations Reports</h2><p>Saved daily operational summaries prepared from dashboard and queue data.</p><b>{d.length} saved reports</b><Link className="btn outline" href="/app/reports/operations">Create Report →</Link></section></div>{d.length>0&&<section className="card saved-reports"><h2>Saved Operations Reports</h2>{d.map(r=><div key={String(r.id)}><span>▥</span><div><b>{r.title}</b><p>{r.summary}</p></div><small>{r.created_at}<br/>{r.created_by}</small><button>Open →</button></div>)}</section>}</div>}
