"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import Link from "next/link";
const roles=["Administrator","Operations Specialist","QA Reviewer","Client Administrator","Candidate","Researcher / Vendor"];
export default function Login(){const r=useRouter(),[email,setEmail]=useState("operations@clearpath.local"),[password,setPassword]=useState("demo123"),[role,setRole]=useState("Operations Specialist"),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch("/api/clearpath/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password,role})});if(res.ok)r.push(role==="Client Administrator"?"/client/dashboard":role==="Candidate"?"/candidate/dashboard":"/app/dashboard");else setError("Email, password, and role did not match a demo account.")}return <main className="login-shell"><section className="login-left"><Link href="/" className="brand inverse"><span className="brand-mark">CP</span><span>ClearPath Screening</span></Link><div><p className="kicker blue">SECURE OPERATIONS ENVIRONMENT</p><h1>ClearPath Screening<br/>Operations Portal</h1><p className="login-sub">Criminal Research · Verification · Quality Review · Compliance · Client Operations</p><p>Secure access to screening orders, research queues, verification requests, quality review, vendor follow-up, billing exceptions, client actions, and operational reporting.</p></div><div className="snapshot"><h3>Current queue snapshot <span>LIVE DEMO</span></h3><div>{[["1,248","Open screening items"],["186","Aging beyond SLA"],["73","Researcher follow-ups due"],["41","Reports in quality review"],["29","Client exceptions"],["14","Billing discrepancies"]].map(x=><div key={x[1]}><b>{x[0]}</b><small>{x[1]}</small></div>)}</div></div><div className="ops-areas">{["New Order Review","Candidate Missing Information","Unassigned Searches","Criminal Research","Employment Verification","Education Verification","Quality Review","Vendor Follow-Up","Billing Reconciliation","Executive Reporting"].map(x=><span key={x}>✓ {x}</span>)}</div></section><section className="login-right"><form onSubmit={submit} className="login-card"><p className="kicker">AUTHORIZED ACCESS ONLY</p><h2>Sign in to ClearPath</h2><p>Use your assigned credentials to access the operations portal.</p><label>Email address<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></label><label>Role<select value={role} onChange={e=>setRole(e.target.value)}>{roles.map(x=><option key={x}>{x}</option>)}</select></label>{error&&<p className="form-error">{error}</p>}<button className="btn primary wide">Sign In →</button><div className="demo-note"><b>Demo environment</b><span>No real candidate or consumer data.</span></div></form><small>© 2026 ClearPath Screening</small></section></main>}
