"use client"; import {useState} from "react";
export default function ResetButton(){const [open,setOpen]=useState(false),[busy,setBusy]=useState(false),[done,setDone]=useState(false);async function reset(){setBusy(true);await fetch("/api/demo/reset",{method:"POST"});setBusy(false);setOpen(false);setDone(true);setTimeout(()=>location.reload(),1200)}return <>{<button className="btn danger" onClick={()=>setOpen(true)}>↻ Reset Demo Data</button>}{open&&<div className="modal-bg"><div className="modal reset-modal"><div className="warning-icon">!</div><h2>Reset Demo Data?</h2><p>This will erase all current demo changes and restore the complete ClearPath demo dataset. Continue?</p>{busy&&<p className="resetting">◌ Resetting ClearPath demo data…</p>}<div className="modal-actions"><button className="btn outline" onClick={()=>setOpen(false)}>Cancel</button><button className="btn danger" onClick={reset} disabled={busy}>Reset Everything</button></div></div></div>}{done&&<div className="toast">✓ ClearPath demo data has been fully restored.</div>}</>}
