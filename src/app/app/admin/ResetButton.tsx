"use client"; import {useState} from "react";
export default function ResetButton(){const [open,setOpen]=useState(false),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");async function reset(){setBusy(true);setError("");try{const response=await fetch("/api/demo/reset",{method:"POST"});if(!response.ok)throw new Error(response.status===403?"Administrator access is required.":"Reset failed. Please try again.");setOpen(false);setDone(true);setTimeout(()=>location.reload(),1200)}catch(e){setError(e instanceof Error?e.message:"Reset failed.")}finally{setBusy(false)}}return <><button className="btn danger" onClick={()=>setOpen(true)}>↻ Reset Demo Data</button>{open&&<div className="modal-bg"><div className="modal reset-modal" role="dialog" aria-modal="true" aria-labelledby="reset-title"><div className="warning-icon">!</div><h2 id="reset-title">Reset Demo Data?</h2><p>This will erase all current demo changes and restore the complete ClearPath demo dataset. Continue?</p>{busy&&<p className="resetting">◌ Resetting ClearPath demo data…</p>}{error&&<p className="form-error" role="alert">{error}</p>}<div className="modal-actions"><button className="btn outline" onClick={()=>setOpen(false)} disabled={busy}>Cancel</button><button className="btn danger" onClick={reset} disabled={busy}>Reset Everything</button></div></div></div>}{done&&<div className="toast" role="status">✓ ClearPath demo data has been fully restored.</div>}</>}
